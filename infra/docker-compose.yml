services:
  ouroboros:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    restart: on-failure
    cpus: 1.0
    mem_limit: 2g
    env_file: ../.env
    volumes:
      - ouroboros-data:/data
volumes:
  ouroboros-data:
